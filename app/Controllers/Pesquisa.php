<?php

include ('../Conection/Conn.php');

        //$pesquisa = filter_input_array(INPUT_POST, FILTER_DEFAULT);
        var_dump($_POST['pesquisar']);exit;

            // $query_usuario = "SELECT * FROM projeto WHERE tit_projeto LIKE '$pesquisa'";
                // $result_usuarios = $pdo->prepare($query_usuario);

                // $result_usuarios->execute();
                

                // while($row_usuario = $result_usuarios->fetch(PDO::FETCH_ASSOC))
                // {
                //   print_r($row_usuario);exit;
                // }
        
    
?>